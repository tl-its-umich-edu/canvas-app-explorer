apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  variant: test
  org: umich
nameSuffix: -test
bases:
- ../../base
patchesJson6902:
#- target:
#    group: extensions
#    version: v1beta1
#    kind: Ingress
#    name: web
#  path: hostname.yaml
- target:
    version: v1
    kind: DeploymentConfig
    name: web
  path: deployment.yaml
- target:
    version: v1
    kind: BuildConfig
    name: web
  path: build.yaml
configMapGenerator:
  - name: canvas-app-explorer
    behavior: replace
    envs:
    - secret/configmap-test.properties